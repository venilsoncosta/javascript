const [a, b, c, d, e] = [10, 18, 34, 98, 103]

const soma = a + b + c + d + e
const subtracao = b - a 
const multiplicacao = b * c 
const divisao = d / a 
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)
